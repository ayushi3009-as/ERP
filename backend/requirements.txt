fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
alembic==1.13.0
psycopg2-binary==2.9.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.0
python-multipart==0.0.6
pydantic==2.5.2
email-validator==2.1.0
pydantic-settings==2.1.0
redis==5.0.1
python-dotenv==1.0.0
reportlab==4.0.8
openpyxl==3.1.2
python-barcode==0.15.1
qrcode==7.4.2
Pillow==10.1.0
httpx==0.25.2
celery==5.3.6
boto3==1.34.0
jinja2==3.1.2
aiosmtplib==3.0.1
pyotp==2.9.0

# Module 18 - AI Production Intelligence: optional LLM provider SDKs.
# Install only the ones your configured AIProviderConfig rows actually
# use -- none of these were installed/verified in the environment this
# module was built in (no network access), so ai_service.call_llm()
# will raise ImportError until whichever of these you need is installed.
openai==1.35.0        # OpenAI + Azure OpenAI
anthropic==0.28.0     # Anthropic Claude
google-generativeai==0.7.0   # Google Gemini
# Ollama uses plain httpx (already a dependency) against its local HTTP API -- no SDK needed

# Monitoring (Phase 12 of the production readiness review)
prometheus-fastapi-instrumentator==6.1.0

# Rate limiting (go-live blocker resolution, Blocker 3)
slowapi==0.1.9
